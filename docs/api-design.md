# parse request

- POST /parse-requests
- GET /parse-requests/{id}

# files

- GET /uploads/{filename}

# parser output

- GET /parse-requests/:request_id/outputs -> list
- GET /parse-requests/:request_id/outputs/:output_id -> details
